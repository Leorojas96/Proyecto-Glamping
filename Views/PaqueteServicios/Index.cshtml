@model IEnumerable<Glamping2.Models.PaqueteServicio>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdPaquetesNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdServiciosNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdPaquetesNavigation.IdPaquetes)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdServiciosNavigation.IdServicios)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPaqueteServicio">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPaqueteServicio">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPaqueteServicio">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
